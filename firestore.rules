rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Custom phone/password authentication bypasses standard Firebase Auth, 
    // so we must allow read/write access to these collections directly.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
